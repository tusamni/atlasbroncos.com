---
interface Props {
	classes?: string;
}

const { classes, ...attrs } = Astro.props;
---

<div class:list={["font-light leading-relaxed tracking-tight text-lg", classes]} {...attrs}>
	<slot />
</div>
